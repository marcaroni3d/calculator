# Calculator

An online alculator with basic arithmetic operations built using HTML, CSS, and Javascript. It can be controlled by a mouse or keyboard.

## Features

- Basic operations supported (add, subtract, multiply, divide)
- Keyboard inputs supported for numbers, operators, and other options.
- Can delete a single input (backspace) or clear the entire display.